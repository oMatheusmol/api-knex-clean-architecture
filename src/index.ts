const nome = 'João';
const sobrenome = 'Silva';

const idade = 23;
const fala = 'oi';

const nomeCompleto = `${nome} ${sobrenome}`;

console.log('Hello World');

const nomeCompleto2 = `${nome} ${idade}`;
